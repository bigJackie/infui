export interface UseContainerOptions {
  vertical?: boolean
  app?: boolean
  absolute?: boolean
}

export function useContainer(options: UseContainerOptions = {}) {
  return options // 暂无逻辑，保留扩展入口
}
